interface Badge {
    name: string;
    badge: string;
}

interface Certificate {
    title: string;
    issuedBy: string;
    instructor: string;
}

interface Hackathon {
    title: string;
    issuedBy: string;
    company: string;
    award: string;
    project: string;
}